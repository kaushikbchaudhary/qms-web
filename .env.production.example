# Next.js production environment
NEXT_PUBLIC_RUNTIME_ENV=production
NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com/
# This should match the backend JWT secret if middleware verification is enabled
NEXT_PUBLIC_JWT_SECRET=replace-with-prod-secret
