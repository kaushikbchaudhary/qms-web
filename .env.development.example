# Next.js development environment
NEXT_PUBLIC_RUNTIME_ENV=development
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001/
# Optional: JWT secret used only by middleware decoding (should match backend when enabled)
NEXT_PUBLIC_JWT_SECRET=replace-with-dev-secret
